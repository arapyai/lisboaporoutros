import { mockAuthors, mockPoints, mockRoutes } from '../api/mock';

export const sampleAuthor = mockAuthors[0];
export const samplePoint = mockPoints[0];
export const sampleRoute = mockRoutes[0];
export const samplePoints = mockPoints;
